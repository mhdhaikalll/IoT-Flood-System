export const metadata = {
    title: 'Authentication',
    description: 'Login or register to access the IoT System Dashboard',
}

const AuthLayout = ({ children }) => {
    return (
        <>
        {children}
        </>
    )
}

export default AuthLayout;